Patient phenotype (verbatim, as submitted): {patient_phenotype}

MOI-layer findings (ground truth — do not deviate from these), grouped by
layer ("=== DE NOVO LAYER ===", "=== DOMINANT-INHERITED LAYER ===", "===
RECESSIVE LAYER ===", "=== X-LINKED LAYER ===", "=== UNCLASSIFIED ==="). A
RECESSIVE layer block is one of TWO kinds — check its title line first:
  - "(compound heterozygous / biallelic)" — a PAIR of variants; embeds both
    variants' own base conclusions verbatim plus its own backend-determined
    "**Phase:**" line (computed in code BEFORE the block was generated — a
    CIS pair never produces this kind of block at all, so the TRANS STATUS
    CHECK below is defense-in-depth, not the primary safeguard).
  - "(confirmed homozygous, biallelic)" — a SINGLE variant; embeds a
    "**Zygosity:**" line instead, never a "**Phase:**" line. There is no
    second variant to check phase against — homozygosity for this one
    variant already satisfies the biallelic requirement on its own, by
    definition. This kind of block is EXEMPT from the TRANS STATUS CHECK
    below (it only applies to the two-variant kind) — never flag it as an
    error for lacking a Phase line or for "unconfirmed" biallelic status.
{conclusions}

ACMG SF actionable-gene findings (ground truth for section 3 below):
{actionable_variants}

Draft clinical conclusion to check:
{draft}

TASK: Check the draft above against the MOI-layer findings for factual
contradictions, then output a corrected version.

Specifically check for:
- VARIANT IDENTITY VERBATIM CHECK (mandatory, do this FIRST, for every variant
  named anywhere in the draft — sections 2, 3, and 4): compare the draft's
  [GENE] ([HGVS]) string against that exact variant's own layer/base block
  header line above, character for character (chromosome/position/ref/alt,
  transcript, cDNA change, and protein change all included). A real past
  failure: the source block's own header read "c.1524G>A:p.W508X"
  (nonsense/stop-gain — the same block's PVS1 justification explicitly cites
  NMD) and the draft wrote "c.1524G>A; p.Glu508Lys" — a fabricated missense
  change contradicting the block's own PVS1 reasoning two sentences later. Any
  character-level mismatch in the HGVS/protein notation, however minor it
  looks (one amino acid, one digit, one nucleotide), is a HARD ERROR — replace
  it with the source block's exact string. Do this independently of every
  other check below; a variant can have a perfectly correct ACMG writeup and
  still fail this check on its identity string alone.
- MISSING ACMG CRITERIA / RE-SCORE CHECK (mandatory, do this for every variant
  listed in section 2 or 4): re-read that variant's OWN full layer/base block
  above in its entirety (prose paragraph, Comment, every field — not just its
  "ACMG criteria:" list) for any statement of evidence that supports a
  criterion NOT already present in the draft's criteria list for that variant
  — most commonly PM2 (population absence/rarity), but check all of PVS1/
  PS1-4/PM1-6/PP1-5/BA1/BS1-4/BP1-7 the same way. A real past failure: a
  variant's own block Comment stated verbatim "The variant is absent from
  population databases, supporting its rarity and pathogenicity" — a direct
  PM2 justification — yet PM2 was absent from that same block's criteria list
  and absent from the draft. If you find evidence in a variant's own block
  text that plainly supports a criterion missing from its criteria list: (1)
  add that criterion to the draft's criteria list for that variant, with the
  correct fixed point value from the table below and a justification quoting
  the block's own supporting text; (2) recompute that variant's total ACMG
  points (old total + the added criterion's value) and update the **ACMG
  classification:** line to match the new total and threshold; (3) if the new
  total crosses the >=6 causative threshold and this variant is not already in
  section 2, move it there per the CAUSATIVE THRESHOLD RULE; (4) update
  section 5's summary sentence if the classification label changed (e.g.
  Uncertain Significance → Likely Pathogenic). Only add a criterion when its
  supporting evidence is explicitly stated in that variant's own block text —
  never add one from general plausibility or gene/disease familiarity alone,
  same grounding bar as every other criterion in this report. Fixed point
  values (Tavtigian et al. 2020) for the added criterion:
    PVS1=+8 (VeryStrong) | PS1/PS2/PS3/PS4=+4 (Strong) |
    PM1/PM2/PM3/PM5/PM6=+2 (Moderate) | PP1/PP3/PP4=+1 (Supporting) |
    BA1=-8 (Standalone) | BS1/BS2/BS4=-4 (Strong) | BP1/BP7=-1 (Supporting)
- CRITERIA LIST COMPLETENESS CHECK (mandatory, for every
  variant listed in section 2): a real past failure — the draft's section 2
  paragraph mentioned a criterion in passing prose (e.g. "...a nonsense
  mutation in a gene where loss-of-function is a known disease mechanism
  (PVS1)...") and then jumped straight to "**ACMG classification:** Pathogenic
  (11 pts total)" with NO structured criteria breakdown at all — no bulleted
  "[CRITERION] ([Strength], +/-N pts): [justification]" list line-by-line.
  A classification line with a point total but no criteria list under it is
  INCOMPLETE, regardless of how well-written the prose paragraph above it is.
  For every causative variant in section 2, verify a criteria list (one
  bullet/clause per applied criterion, each with its strength, point value,
  and justification) is present between the prose paragraph and the
  **ACMG classification:** line. If it is missing, ADD it — copy the full
  criteria list verbatim from that variant's own layer/base block above (the
  "ACMG criteria:" or "Base ACMG criteria" lines), do not summarize it into
  prose or invent one. This is the single most important check in this list.
- Any ACMG criterion (PVS1, PS1-4, PM1-6, PP1-5, BA1, BS1-4, BP1-7...) named
  in the draft that does NOT appear in the cited variant's own layer block
  (or its embedded base conclusion).
- Any ACMG classification or point total in the draft that does not exactly
  match the "ACMG points:" / "Total ACMG points:" line in that variant's own
  layer block.
- Any inheritance/segregation claim (de novo, maternal/paternal, carrier
  status) in the draft that conflicts with what that variant's own layer
  block states.
- Any other factual claim in the draft not traceable to the layer findings
  above.
- EXCLUDED-VARIANT PHENOTYPE-MATCH CHECK (mandatory, do this for every gene
  the draft's Reasoning/STEP 0 section describes as NOT matching the
  phenotype, lacking phenotype specificity, or otherwise not explaining the
  presentation — even when that gene is correctly excluded from section 2):
  read that gene's OWN layer/base block above and check whether it lists
  PP4 among its applied criteria. A real past failure: the draft wrote
  "homozygous GENE_X findings ... without phenotype match, failing to
  explain the [phenotype]" for a gene whose own block explicitly applied
  "PP4 Supporting (+1 pts): Patient phenotype ... is highly specific for
  GENE_X-associated disease" a few paragraphs earlier in the same layer
  findings — a direct self-contradiction. If the excluded gene's own block
  DOES apply PP4, the draft may still exclude it from section 2 (e.g. for
  insufficient segregation, weaker overall classification, or a competing
  stronger finding), but it MUST NOT claim "no phenotype match" — rewrite
  that sentence to state the real reason for exclusion (e.g. missing
  segregation/PM3 confirmation, lower point total than the chosen variant)
  instead of a phenotype-match denial the layer block itself contradicts.
- MOI-LAYER TAG CONFIRMATION CHECK (mandatory, do this for every variant in
  section 2 tagged "De novo" or "Dominant-inherited"): a variant is routed
  into the DE NOVO LAYER or DOMINANT-INHERITED LAYER simply because its gene
  is AD/AD_AR/XLD — landing in that layer is NOT itself proof the specific
  mechanism was confirmed. Read that variant's own DE NOVO/DOMINANT-INHERITED
  layer block: if tagged "De novo", its "De novo criteria applied:" line
  must name PS2 or PM6 (delta > +0); if tagged "Dominant-inherited", its
  criteria-applied line must show PP1 applied (delta > +0). If the block
  instead says "None — de novo status not confirmed/not assessable" (or the
  dominant-layer equivalent) with delta +0, the tag is WRONG — replace it
  with "AD candidate, de novo unconfirmed" (or "... unassessed" if Parental
  data: none) / "AD candidate, cosegregation unconfirmed", and fix any prose
  elsewhere in the draft (including section 5's summary sentence) that
  asserts de novo/inherited status as confirmed for that variant.
- Two (or more) variants in the SAME gene, both with supporting evidence and
  segregation consistent with biallelic inheritance (compound heterozygous),
  that the draft frames as competing with each other (e.g. one "superior" or
  "primary" over the other) instead of jointly causative. If found, rewrite
  section 2 to list all such variants as co-causative, each preceded by its
  gene name, per a compound-het/biallelic recessive model — do not keep a
  single-winner framing for a gene with two qualifying trans variants.
- DE NOVO + X-LINKED SAME-GENE CHECK: if both a DE NOVO layer block and an
  X-LINKED layer block exist for the same gene/variant, they are not
  competing diagnoses — a confirmed de novo occurrence (PS2) is independent,
  stronger evidence of dominant mechanism than X-linked cosegregation (PP1,
  which requires an affected mother and is irrelevant once de novo is already
  confirmed). If the draft cites the X-LINKED layer's (lower) score/
  classification for this variant instead of the DE NOVO layer's, correct it
  to use the DE NOVO layer's total score and classification, and do not
  present the X-linked layer's lower classification as a separate or
  competing finding.
- TRANS STATUS CHECK (recessive genes only, MANDATORY — do this for every
  gene the draft calls "compound heterozygous" or "biallelic" as a PAIR of
  two distinct variants; do NOT run this for a gene whose RECESSIVE block is
  the "(confirmed homozygous, biallelic)" single-variant kind — see above,
  it is exempt and its biallelic status is already correct — even if
  everything else about the draft looks correct):
  Step A: if a RECESSIVE layer block exists for that gene, read its own
  "**Phase:**" line first — this is the authoritative, already-verified
  value. Only if no RECESSIVE block exists for that gene (the draft is
  claiming compound-het without one), fall back to finding each variant's
  Segregation/parental-origin line within its own embedded base conclusion
  and copy them EXACTLY as written, e.g.:
    Variant 1 Segregation: "inherited from mother"
    Variant 2 Segregation: "inherited from mother"
  Step B: compare the two parent-of-origin values as plain strings.
    - Both say the same single parent (e.g. both "mother", or both
      "father") → CIS. The draft's compound-het claim is WRONG — this is a
      HARD ERROR, not a style issue, regardless of how strong the ACMG
      evidence or gene-disease mechanism looks. Two variants inherited from
      the same parent sit on one allele; the other parent's allele is
      wild-type at both positions, so biallelic/recessive inheritance is
      NOT satisfied.
    - Different parents (one "mother", one "father") → TRANS. Compound-het
      claim is correct, leave as-is.
    - One or both are "de novo" / "unknown" / "no parental data" → phase
      UNKNOWN. Compound-het claim may stand, but the draft must note the
      phase uncertainty.
  Step C: if Step B found CIS, rewrite section 2 to drop the compound-het/
  biallelic framing for that gene — fall back to a single causative variant
  (pick the one with the stronger ACMG classification) or state the gene
  has insufficient biallelic evidence, per the per-variant conclusions. Also
  fix section 3's summary sentence to match (remove "compound heterozygous"
  wording for that gene).
- CAUSATIVE-THRESHOLD OMISSION CHECK (mandatory, do this for EVERY MOI layer
  block above regardless of layer type — De Novo, Dominant-Inherited,
  Recessive, X-Linked, Unclassified): the rule is a single uniform numeric
  cutoff — any variant whose own layer/base block states a total ACMG point
  value >= 6 (Likely Pathogenic or Pathogenic, Tavtigian et al. 2020) MUST
  appear in section 2. Scan every layer block's own total-points line and
  compare against section 2's variant list. A real past failure: a RECESSIVE
  layer variant scored 9.5 points (Likely Pathogenic, e.g. via a reduced-
  weight PM3 for unconfirmed segregation) and was left out of section 2
  entirely — reasoning-only mention doesn't count — because a DIFFERENT
  gene's variant scored higher (e.g. 11 points, Pathogenic) and the draft
  treated that as a single winner. Scoring higher than another qualifying
  variant is NEVER a valid reason to omit one that independently clears the
  >=6 bar — both are independently causative unless one is excluded by one of
  the OTHER named checks in this list (TRANS/CIS collapse, SINGLE-HIT
  RECESSIVE CHECK, a stated factual inconsistency in its own block). If you
  find a >=6-point variant missing from section 2, ADD its full write-up
  (prose paragraph, criteria list copied verbatim from its own block, ACMG
  classification line) — do not just name it in passing. Also fix section 5's
  summary sentence to name every causative gene, not only the previously-sole
  one.
- SINGLE-HIT RECESSIVE CHECK (mandatory, do this for every gene the draft
  lists as causative with only ONE variant, even if everything else about
  the draft looks correct): find that gene's inheritance mode and this
  variant's zygosity/compound-het status in its own per-variant conclusion.
  If the gene is autosomal recessive (AR) or X-linked recessive (XLR), the
  variant is heterozygous, and no second variant/trans partner is
  documented for that gene — the draft's causative claim for this variant
  is WRONG, regardless of how strong its ACMG evidence looks. Remove this
  variant from section 2. If no causative variant remains after removal,
  state explicitly that no confirmed causative variant was found under a
  recessive model (insufficient evidence — single heterozygous hit only),
  and fix section 3's summary sentence to match.
- DISEASE-NAME CHECK (mandatory, do this for EVERY gene the draft names as
  causative, even if everything else about the draft looks correct): find
  the specific disease/condition name the draft attributes to that gene in
  section 2's prose (e.g. "the gene is a known cause of X"), and verify that
  exact disease name appears in THAT gene's own layer block / embedded base
  conclusion above — not in a different gene's block, and not merely implied
  by a shared category phrase used earlier in the draft (e.g. "visual
  impairment (retinitis pigmentosa or cone-rod dystrophy)" in the reasoning
  section does NOT license attributing "retinitis pigmentosa" to every gene
  discussed nearby — each gene keeps only the specific disease its own
  evidence names). When multiple genes are listed together and share a broad
  phenotype category, it is common for the draft to blur one gene's specific
  named disease into a neighboring gene's paragraph — this is a HARD ERROR,
  not a style issue, even when the borrowed disease name is phenotypically
  plausible for the category. If a gene's paragraph names a disease not
  found in that gene's own layer block, replace it with whatever disease
  name (if any) that gene's own block actually states, or remove the
  specific disease-name claim entirely if its own block doesn't name one.

- PP4 SPECIFICITY CHECK (mandatory, do this for every variant in section 2
  whose criteria list includes PP4): re-read the "Patient phenotype (verbatim,
  as submitted)" line at the top against that gene's own layer/base block. PP4
  requires BOTH: (1) single-gene etiology — the disease is not known to be
  caused by other genes too; (2) discriminating specificity — the patient's
  phenotype includes a feature that is itself a recognized hallmark of this
  specific disease, not a generic symptom shared by hundreds of syndromes
  ("intellectual disability", "developmental delay", "seizures", "short
  stature" in isolation never qualify). A real past failure: the submitted
  phenotype was the single generic term "intellectual disability" and PP4 was
  applied with the reasoning "a core feature of the [GENE]-associated
  syndromic neurodevelopmental disorder" — circular (every candidate gene in
  a multi-gene panel run shares that same generic phenotype). A second,
  near-identical recurrence: the submitted phenotype was "intellectual
  disability. father and mother unaffected" and PP4 was applied again with
  the same reasoning — segregation/pedigree facts (parents affected/
  unaffected, family history of the SAME condition, consanguinity) are NOT
  clinical features and do NOT satisfy condition (2); they bear on
  inheritance model, not on what disease this patient has. Strip any
  segregation/pedigree wording from the submitted phenotype before judging
  this check — if what remains is only a generic disease-category term (alone
  or combined with other generic terms) with no actual distinguishing
  clinical finding, condition (2) fails by construction, no matter how strong
  the gene-disease literature link otherwise looks. If so, remove PP4 from
  every variant's criteria list in section 2 and 4, recompute that variant's
  point total and classification accordingly (a variant may drop out of
  "causative" entirely if PP4 was the deciding point), and update the summary
  sentence to match. Do this for EVERY variant with PP4, not just the first
  one you check.
- BENIGN/OUT-OF-SCOPE MENTION CHECK (mandatory, scan the ENTIRE # Clinical
  Conclusion section): the only variants permitted anywhere in that section
  are causative variants (section 2), ACMG SF actionable findings from the
  ground-truth list above (section 3), and VUS variants with exactly 4 or 5
  total ACMG points per their own layer/base block (section 4). If the draft
  names, describes, or even mentions-as-excluded any variant classified
  Benign or Likely Benign, or any VUS outside the 4-5 point range, delete
  that mention entirely (do not replace it with an explanation of why it was
  removed — just remove it, as if it were never written).
- ACTIONABLE SECTION CHECK (mandatory): every variant listed in section 3
  must appear in the ACMG SF ground-truth list above, with matching gene,
  condition, and classification. Remove any section-3 entry not in the
  ground-truth list. Add any ground-truth entry missing from section 3
  (using the brief one-line format, or a cross-reference to section 2 if the
  same variant is already written up there). If the ground-truth list says
  "None" and section 3 lists variants anyway, replace section 3 with "None
  identified."
- NOTABLE VUS CHECK (mandatory): every variant listed in section 4 must have
  a layer/base block above stating a total ACMG point value of exactly 4 or
  5 AND a classification of "Uncertain significance"/"VUS". Remove any
  section-4 entry whose own block shows a different point total or a
  different classification. If section 4 is missing while a qualifying 4-5
  point VUS exists in the layer findings above, add it.

If you find no contradictions, output the draft unchanged.
If you find contradictions, output a corrected version with the same
structure (# Reasoning, # Clinical Conclusion, same numbered sections) —
fix only the incorrect claims, keep everything else as written.

Output ONLY the final corrected text (# Reasoning ... # Clinical Conclusion
...). Do not explain what you changed, do not add commentary before or
after it.
