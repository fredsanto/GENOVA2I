Patient phenotype: {patient_phenotype}
Per-variant conclusions: {conclusions}

TASK: Identify which variant(s) best explain the patient's phenotype.

Ground your reasoning in the per-variant conclusions above. For each
claim you make about a variant, that claim should be traceable to
something stated in its conclusion (zygosity, inheritance, gene-disease
evidence, ClinVar status, etc.).

STEP 1 — PHASE CHECK (mandatory, do this first, before choosing a model):
This step ONLY applies to a gene with TWO OR MORE DISTINCT candidate
variants. Do NOT run it, and do NOT write a Phase check line, for a gene
with only one candidate variant — this includes a single CONFIRMED
HOMOZYGOUS variant, whose Recessive layer block is titled "confirmed
homozygous, biallelic" and has a **Zygosity:** line, never a **Phase:**
line. There is no second, different variant to compare parent-of-origin
against in that case, so CIS/TRANS phase is not a meaningful concept —
homozygosity for the single variant already satisfies the biallelic
requirement on its own, by definition, with no phase check needed. Skip
straight to STEP 2 for that gene (SINGLE CAUSATIVE VARIANT — see the
"confirmed homozygous" bullet there).

For every gene with two or more candidate variants, extract each variant's
parent-of-origin EXACTLY as written in its own per-variant conclusion's
Segregation/inheritance line, and write one line per variant in this literal
format:
  Phase check [GENE]: Variant A parent-of-origin = <mother/father/de novo/
  unknown>; Variant B parent-of-origin = <mother/father/de novo/unknown> →
  <SAME parent = CIS | DIFFERENT parents = TRANS | UNKNOWN>
Do this as plain string comparison of the two parent-of-origin values — do
not reason about anything else yet. You MUST write this line even if you
are confident of the answer.

Worked example (this exact pattern has caused errors before — copy this
reasoning pattern exactly):
  Variant 1 Segregation: "inherited from mother"
  Variant 2 Segregation: "inherited from mother"
  Phase check GENE: Variant 1 parent-of-origin = mother; Variant 2
  parent-of-origin = mother → SAME parent = CIS
  → Both variants sit on the single allele inherited from the mother. The
  allele inherited from the father is wild-type at both positions. This is
  NOT compound heterozygous, regardless of how strong each variant's
  individual ACMG evidence is or how well the gene's biallelic disease
  mechanism fits the phenotype. Do not use the COMPOUND HETEROZYGOUS model
  for this gene.

STEP 2 — Determine the inheritance model from the per-variant conclusions,
using the STEP 1 phase check result:
- COMPOUND HETEROZYGOUS / BIALLELIC RECESSIVE: two (or more) variants in the
  SAME gene, each with supporting evidence (ACMG classification of at least
  Likely Pathogenic, or VUS with strong supporting criteria), AND the STEP 1
  phase check for that gene concluded TRANS (or UNKNOWN — phase not stated).
  In this case ALL such variants are jointly required to explain the
  phenotype — do not rank one over the other or frame it as one variant
  being "superior" to the other. Both/all must be listed as co-causative.
  If the phase check concluded UNKNOWN, you may still use this model but
  must note the phase uncertainty in the Reasoning section.
  If the STEP 1 phase check for that gene concluded CIS, you MUST NOT use
  this model for that gene, no matter how strong the individual variant
  evidence or gene-disease mechanism looks — fall back to SINGLE CAUSATIVE
  VARIANT (pick the stronger of the two) or state the gene has insufficient
  biallelic evidence.
- SINGLE CAUSATIVE VARIANT: one dominant, de novo, or clearly strongest
  variant with no qualifying second allele in the same gene (including the
  case where STEP 1 found CIS phase for what looked like a compound-het
  pair). Only this case gets a single named variant.
  This category also covers a single CONFIRMED HOMOZYGOUS variant in an
  AR/XLR gene (Recessive layer block titled "confirmed homozygous,
  biallelic") — it is fully causative on its own; biallelic status is
  satisfied by the homozygous zygosity itself, not by a second variant or a
  TRANS phase finding. Do NOT state that biallelic status is "unconfirmed"
  or demand a second variant/trans partner for this case.

SINGLE-HIT RECESSIVE CHECK (applies to every gene, including genes with only
one candidate variant — do this before finalizing section 2): for each
variant you are about to list as causative, check its own per-variant
conclusion's Inheritance check / Segregation / compound-het fields. If that
gene's inheritance mode is autosomal recessive (AR) or X-linked recessive
(XLR), the variant is heterozygous, and no second variant in that gene was
identified (no compound-het/trans partner, and the variant itself is not
homozygous) — do NOT list this variant as causative, no matter how strong
its individual ACMG classification is. Instead state that this gene has
insufficient evidence for recessive disease causation (single heterozygous
hit only) and exclude it from section 2. If no variant remains after this
check, section 2 must say so explicitly rather than naming an insufficient
variant. This check is independent of, and applied before, the compound-
heterozygous STEP 1/STEP 2 logic above (which only concerns genes with two
or more candidate variants).

Then write the clinical conclusion.

# Reasoning
[Start with the STEP 1 Phase check line(s) for any multi-variant gene, then
3–6 sentences comparing the variants, drawing on what each conclusion
states, and explicitly stating which inheritance model above applies]

# Clinical Conclusion

1) Summary of clinical phenotype: [1–2 sentences summarizing the patient's phenotype as given]

2) Causative variant(s): list every variant identified as causative above.
   If compound heterozygous/biallelic, list each variant on its own,
   ALWAYS preceded by its gene name, and do not present them as competing
   with each other. If a single causative variant, list only that one.

   [GENE] ([HGVS])

   Write a short prose paragraph (2-4 connected sentences, not bare bullet
   fragments) covering, in this order, whichever of the following apply:
   segregation/inheritance status (de novo, inherited maternal/paternal, or
   both parents carriers if recessive — include parental carrier/zygosity
   status when available in the per-variant conclusion; state "no parental
   data" if absent); population frequency (rare/absent from controls, per
   PM2/BA1/BS1 evidence); mutational hot spot / critical domain status (per
   PM1 evidence), if applicable; loss-of-function mechanism (per PVS1
   evidence), if applicable; relevant publications on this variant, with
   PMIDs, if cited in its conclusion; and why this variant (jointly with its
   trans partner, only if STEP 1 confirmed TRANS phase and compound het
   applies) explains the patient's phenotype.

   Then, on its own line: **ACMG classification:** [classification] ([N] pts
   total). List every applied criterion as its own clause stating its
   strength AND its individual point value, not just its name, e.g.
   "PVS1 (Very Strong, +8 pts): loss-of-function variant in a gene with a
   known LoF disease mechanism; PM2 (Moderate, +2 pts): absent from
   population databases; PP3 (Supporting, +1 pt): computational evidence
   (REVEL=0.82) supports a deleterious effect." Copy each criterion, its
   strength, its point value, and its one-sentence justification verbatim
   from this variant's own per-variant conclusion block above — do not
   recompute, re-derive, restate the point totals differently, or drop any
   applied criterion. A bare comma-separated criteria list with no points
   (e.g. "PVS1, PM2, PP3") is not sufficient.

   [repeat the block above for each additional causative variant, if compound heterozygous/biallelic]

3) Summary: [one sentence tying together the gene(s), inheritance model
   (single dominant/de novo vs compound heterozygous), and ACMG
   classification(s) into a final overall finding — e.g. "These findings
   support a diagnosis of [condition] due to compound heterozygous
   pathogenic variants in [GENE]." Ground it only in what sections 1–2
   already state, no new evidence.]

Ground every claim in this section in the per-variant conclusion above (ACMG criteria, segregation, citations) — do not introduce new evidence.
