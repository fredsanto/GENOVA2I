
PATIENT PHENOTYPE:
{patient_report}

VARIANT DATA:
{variant}
{prior_evidence_block}
TASK:

PubMed literature for this gene and phenotype has already been retrieved by prior tools
(see pre-fetched evidence block). Do NOT repeat general gene-disease PubMed searches.

Search specifically for complementary evidence not covered by prior tools:
1. OMIM or GeneReviews page for this gene — gene function, disease mechanism, allelic variants
2. Full ClinVar record for THIS RESIDUE — search by the protein/amino-acid POSITION (e.g. "CRB1 p.Cys1321 ClinVar", destination amino acid dropped), NOT the nucleotide change and not the full 3-part change. A nucleotide-level query (e.g. "c.3962G>C") only ever finds this exact allele; a position-only query surfaces EVERY substitution reported at that residue — both a different nucleotide producing the same amino acid change (needed for PS1) and a different amino acid change altogether at the same residue (needed for PM5). For each hit found, report its exact amino acid change AND its own nucleotide notation alongside the finding, so downstream: same nucleotide = this variant's own record (supports PS3 if functional data is cited, not PS1/PM5); different nucleotide + same amino acid change = supports PS1 if classified pathogenic/likely pathogenic; different amino acid change = supports PM5 if classified pathogenic/likely pathogenic.
3. Recent preprints (bioRxiv, medRxiv) or clinical studies not yet in PubMed — search by gene + protein change
4. Functional or experimental data for this specific variant or protein position

IMPORTANT: When searching for a specific variant, search by the protein/residue change (gene + p.xxx, 3-letter
amino-acid form, e.g. "CRB1 p.Cys1321Ser") rather than the nucleotide change — this is what surfaces same-residue
precedents needed for PS1. Fall back to the nucleotide notation (gene + c.xxx) only for variants with no protein
change (intronic/splice). Do NOT use rsIDs — they return poor web search results.

Use web_search to locate sources, then web_fetch or ncbi_fetch to read the most relevant ones.
Limit to 3 focused searches. Stop as soon as the key gaps above are filled.

Output format:
- One bullet per finding, 1-2 sentences max
- Include the full URL for each source
- State clearly if a source was not found or returned no relevant data
