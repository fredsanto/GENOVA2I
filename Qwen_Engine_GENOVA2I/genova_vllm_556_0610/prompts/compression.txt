Condense the following variant evidence to {max_tokens} tokens maximum, producing a clean text for downstream processing.

Rules:
- Work strictly from the text provided. Do not add, infer, or extrapolate.
- Keep every distinct evidence section as its own labeled block. Do not merge sections, even if they discuss the same gene or variant.
- Preserve verbatim: section headers, ACMG criterion codes with strength modifiers (e.g. PM2_Supporting), HGVS notation, variant classifications (P/LP/VUS/LB/B), gene-disease associations including explicit negative findings ("gene X not linked to disease Y"), and all unique URLs.
- When evidence links the gene to only PART of a multi-feature phenotype (affirmed for one clinical feature/cluster, silent on another), preserve that as a PARTIAL/mixed finding: keep the affirmative link as the lead clause and the gap as a trailing caveat, in that order. Never restate a partial finding as a negative one ("no evidence links gene to phenotype") when an affirmative link to any part of the phenotype exists in the source text — that changes the finding's meaning, not just its length. This overrides the "keep every section separate" rule above when the source text has already split a single partial finding into two separately-labeled sections (e.g. a "Gene-Disease Link:" bullet affirming one cluster and a separate "Phenotype Mismatch:" bullet noting silence on another) — merge those two into one combined partial-match statement instead of preserving "Mismatch" as its own standalone section; a section titled "Mismatch"/"No link" sitting next to an affirmative link is itself the exact negative-restatement this rule forbids, just split across two labels instead of one sentence.
- Remove: repeated information, verbose phrasing, and raw data already interpreted in the text.
- If the content cannot fit within {max_tokens}, shorten prose first; never drop a section, a URL, or an ACMG code to make room.

Evidence:
{variant_report}