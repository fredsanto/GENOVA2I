name: litvar2_summary
description: "Fetches variant-specific PubMed articles via LitVar2, filters with SLM, returns evidence summary."
enabled: true

order: 1
parallel: true

compress:
  enabled: true
  threshold: 20
  strategy: slm
  max_tokens: 600

timeout: 15
retry:
  attempts: 2
  backoff: 2.0
